name: Automated tests

on:
  workflow_call:

env:
  NODE_VERSION: 22

jobs:
  run-unit-tests:
    name: 🧪 Unit tests
    timeout-minutes: 5
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup turbo cache
        uses: actions/cache@v5
        with:
          path: .turbo
          key: unit-tests-turbo-${{ github.sha }}
          restore-keys: |
            unit-tests-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run unit tests with coverage
        run: |
          pnpm run test:ci -- --coverage
        env:
          CI: true
          VITE_BASE_URL: http://example.com

      - name: Push main app test report to artifacts
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v6
        with:
          name: vitest-report-main-app
          path: apps/main/vitest-report/
          retention-days: 14

  playwright:
    name: 🎭 E2E tests (${{ matrix.browser }})
    runs-on: ubuntu-latest
    timeout-minutes: 5

    strategy:
      matrix:
        browser: [chromium]

    steps:
      - uses: actions/checkout@v6

      - name: Setup kernel for react native, increase watchers
        run: echo fs.inotify.max_user_watches=524288 | sudo tee -a /etc/sysctl.conf && sudo sysctl -p

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Setup turbo cache
        uses: actions/cache@v5
        with:
          path: .turbo
          key: ${{ matrix.browser }}-e2e-tests-turbo-${{ github.sha }}
          restore-keys: |
            ${{ matrix.browser }}-e2e-tests-turbo-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Install ${{ matrix.browser }} browser
        run: npx playwright install --with-deps -- ${{ matrix.browser }}

      - name: Install serve package
        run: pnpm install -g serve

      - name: Run production build
        run: pnpm run build
        env:
          CI: true
          VITE_BASE_URL: http://example.com

      - name: Run tests
        run: pnpm run e2e:ci -- --browser=${{ matrix.browser }}
        env:
          CI: true
