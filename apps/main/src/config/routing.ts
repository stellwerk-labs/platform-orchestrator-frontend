export interface MatchParams {
  orgId: string;
  projectId: string;
  envId: string;
  deploymentId: string;
  moduleId: string;
  runnerId: string;
  resourceTypeId: string;
  providerId: string;
  providerType: string;
  deviceLoginCode: string;
  userId: string;
  serviceUserId: string;
}
