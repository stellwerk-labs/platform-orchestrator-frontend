/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 */
import type { ServerlessEcsRunnerJobEnvironment } from './serverlessEcsRunnerJobEnvironment';
import type { ServerlessEcsRunnerJobSecrets } from './serverlessEcsRunnerJobSecrets';

/**
 * Job details for the AWS ECS Runner
 */
export interface ServerlessEcsRunnerJob {
  /**
   * AWS Region to run the ECS job in.
   *
   * @minLength 1
   * @maxLength 24
   * @pattern ^[a-z]{2}-[a-z]+-\d$
   */
  region: string;
  /**
   * ECS Fargate cluster name
   *
   * @minLength 1
   * @maxLength 255
   */
  cluster: string;
  /**
   * The IDs of the AWS subnets that the runner must be connected to. At least one is required.
   *
   * @minItems 1
   * @maxItems 16
   */
  subnets: string[];
  /**
   * An optional list of IDs for the AWS security groups that the runner should be associated with.
   *
   * @maxItems 5
   */
  security_groups?: string[];
  /** Whether to assign a public ip to the ECS task. This is required if the target subnets use Internet Gateways
instead of NAT Gateways to reach public networks. Defaults to false.
   */
  is_public_ip_enabled?: boolean;
  /**
   * The ARN of the role used to pull images, retrieve secrets and parameters, and execute the Runner.
   *
   * @pattern ^arn:aws:iam::\d{12}:role\/[a-zA-Z_0-9+=,.@\-_/]+$
   */
  execution_role_arn: string;
  /**
   * Container image to use for the runner. If not specified, a default runner image will be used.
   *
   * @maxLength 255
   * @pattern ^[a-zA-Z0-9._:/-]+(?:@[a-z0-9]+:[a-fA-F0-9]+)?$
   */
  image?: string;
  /**
   * An optional ARN for the role available inside the Runner.
   *
   * @pattern ^arn:aws:iam::\d{12}:role\/[a-zA-Z_0-9+=,.@\-_/]+$
   */
  task_role_arn?: string;
  /** Additional environment variables to pass to the ECS job. */
  environment?: ServerlessEcsRunnerJobEnvironment;
  /** A set of AWS Secrets Manager Secrets or Parameter Store Parameters to mount */
  secrets?: ServerlessEcsRunnerJobSecrets;
}
